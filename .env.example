PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=validafifo
DB_USER=postgres
DB_PASSWORD=postgres

# Use true ao conectar em um banco PostgreSQL na nuvem que exige SSL (ex.: Render, Railway)
DB_SSL=false
